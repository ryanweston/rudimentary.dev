import { defineConfig } from 'unocss'

export default defineConfig({
  theme: {
    fontFamily: {
    //   sans: ['Inter var', 'Inter', 'sans-serif'],
      sans: ['IBM Plex Mono'],
      mono: ['IBM Plex Mono'],
    },
  },
})
